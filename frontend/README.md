# Affinity Frontend

This directory contains the Next.js prototype UI for Affinity Proto.

For the full project overview, screenshots, setup instructions, backend notes, and validation commands, see the root [`README.md`](../README.md).

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

The local frontend runs at:

```text
http://localhost:3000
```

